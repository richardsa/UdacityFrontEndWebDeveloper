import { handleSubmit } from "../src/client/js/formHandler";
describe("Testing if defined", () => {
  test("Testing handleSubmit() function exists", () => {
    expect(handleSubmit).toBeDefined();

  })
});
